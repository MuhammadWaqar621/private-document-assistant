# Duplicated from ../backend/requirements.txt - Vercel's Python builder
# doesn't support pip's `-r ../other/requirements.txt` include syntax
# (tried it, it fails to parse), so this has to be a real copy, not a
# reference. Keep in sync manually when backend/requirements.txt changes.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic[email]==2.10.4
pydantic-settings==2.7.1
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
pgvector==0.3.6
openai==1.59.6
python-dotenv==1.2.2
python-multipart==0.0.31

# Auth (Phase 2): password hashing, JWT, forgot-password email
# bcrypt is pinned <4.1: passlib 1.7.4's internal wrap-bug self-test
# crashes with a hard ValueError on bcrypt>=4.1 (bcrypt 5.x confirmed
# broken here) instead of the ValueError passlib expects to catch.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.4.0
httpx==0.28.1
aiosmtplib==5.1.2

# Document ingestion / RAG engine (this phase): PDF/DOCX text extraction
pdfplumber==0.11.4
python-docx==1.1.2

# OCR (images + scanned PDF pages) is NOT installed in this deployment:
# EasyOCR pulls in torch/torchvision, which alone push the Vercel Python
# function bundle past its 500MB limit (measured: 1.28GB with it in).
# app/engine/extraction.py degrades gracefully when easyocr isn't
# importable - scanned pages/images get a placeholder instead of a crash.
# To restore OCR, run this app on a host without a function-size limit
# (e.g. a normal VM/container) and add `easyocr` back here.
#
# pymupdf (import name `fitz`) renders a scanned PDF page to an image for
# OCR with no system binary needed - kept even without OCR since it's
# small and extraction.py's rendering path is still exercised/tested.
pymupdf==1.25.1

# Testing (backend/tests/): FastAPI's TestClient needs httpx (already
# present above); pytest-asyncio is included for any future async test
# functions even though the current suite is entirely synchronous
# (TestClient wraps async endpoints transparently).
pytest==9.0.3
pytest-asyncio==1.4.0
